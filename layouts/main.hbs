<!DOCTYPE html>

<html lang='en'>

<head>
  <meta charset='UTF-8' />
  {{#if notitle}}
  <title>{{site.name}}</title>
  {{else}}
  <title>{{site.name}} - {{title}}</title>
  {{/if}}
  <meta name='description' content='{{site.description}}' />
  <meta name='Author' content='{{site.author}}' />
  <meta name='viewport' content='width=device-width, initial-scale=1.0' />
  <meta name='generator' content='metalsmith' />

  <meta property="og:title" content="{{sitename}} - {{title}}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="{{site.url}}">
  <meta property="og:description" content="{{social_description}}">
  <meta property="og:image" content="{{social_image}}">

  <link rel="icon" href="/images/favicon.ico">
  <link rel="icon" href="/images/favicon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/images/favicon.png">

  <link rel="preload" href="/fonts/opensans.css" as="style" onload="this.onload=null;this.rel='stylesheet'" />
  <noscript>
    <link rel="stylesheet" href="styles.css" />
  </noscript>

  <link rel="preload" href="/css/main.css" as="style" onload="this.onload=null;this.rel='stylesheet'" />
  <noscript>
    <link rel="stylesheet" href="styles.css" />
  </noscript>
</head>

<body>
  <div class='container'>
    <header>
      <nav>
        <h1>{{sitename}}</h1>
        <a href='/'>Home</a>
        <a href='/posts'>Posts</a>
        <a href='/info'>Info</a>
      </nav>

      <h1>{{title}}</h1>
      {{#if subtitle}}
      <h2>{{subtitle}}</h2>
      {{/if}}

      {{#if date}}
      <span>{{date}}</span>
      {{/if}}
    </header>
    <main>
      {{{contents}}}
      {{#if postlist}}
      <ul>
        {{#each collections.posts}}
        <li>
          <a href="/{{ path }}">[{{date}}] {{ title }}</a>
        </li>
        {{/each}}
      </ul>
      {{/if}}
    </main>
    <footer>
      <p> Copyright &copy; {{buildyear}} {{author}} </p>
      <p> Made with <a href="https://metalsmith.io/">Metalsmith</a> </p>
    </footer>
  </div>
</body>

</html>
