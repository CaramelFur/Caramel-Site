{
  "name": "rubikscraft-site",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "yarn build && node ./lib/deploy.js ./deploy.config.json"
  },
  "type": "commonjs",
  "dependencies": {
    "markdown-it-image-lazy-loading": "^1.2.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-canary.16",
    "easy-sftp-deploy": "^1.3.9",
    "eleventy-sass": "^2.1.3",
    "html-minifier": "^4.0.0"
  }
}
